const path = require('path');
const fs = require('fs');
// const arrFolders = fs.readdirSync('./webpack/entries');
// const extension = {};

// fs.readdirSync(arrFolders).forEach(file => {
//   extension[(path.extname(fileNames).substr(1)).toString][file.toString];
// });

// modules.exports = extension;
